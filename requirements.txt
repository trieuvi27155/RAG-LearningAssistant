streamlit>=1.32
pdfplumber>=0.11
python-pptx>=0.6.23
python-docx>=1.1
langchain-text-splitters>=0.2
sentence-transformers>=2.7
# torch đi kèm sentence-transformers, nhưng bản cài mặc định từ PyPI là CPU-ONLY. Trên máy
# CÓ GPU NVIDIA, đó là một cấu hình sai KHÔNG gây lỗi: hệ thống vẫn trả lời đúng, chỉ chậm
# hơn nhiều lần (đo được: embedding 13,3x, rerank 5,0x - xem KET_QUA_DO_DAC.md §7.4).
#
# Muốn dùng GPU thì cài BẢN CUDA, thay <cuXXX> bằng phiên bản khớp driver của bạn
# (`nvidia-smi` in ra "CUDA Version"; cu126/cu128/cu130 là các mốc phổ biến):
#
#     pip install torch --index-url https://download.pytorch.org/whl/cu130
#
# Kiểm tra sau khi cài:  python -c "import torch; print(torch.cuda.is_available())"
# GPU dòng RTX 50xx (Blackwell) cần CUDA >= 12.8, tức cu128 trở lên.
# KHÔNG có GPU thì bỏ qua toàn bộ mục này - hệ thống tự chạy CPU, không cần chỉnh gì.
faiss-cpu>=1.8
ollama>=0.2
# httpx vốn là dependency của ollama, nhưng rag/rag_pipeline.py bắt httpx.ConnectError một
# cách TƯỜNG MINH (đường streaming của ollama không bọc lỗi kết nối lại) nên nó là
# dependency trực tiếp, phải khai báo ở đây thay vì trông vào cây phụ thuộc của gói khác.
httpx>=0.27
tiktoken>=0.7
langdetect>=1.0.9
numpy>=1.26

# --- Chỉ cần cho phát triển/đánh giá, không cần để chạy ứng dụng ---
pytest>=8.0
# Sinh bộ tài liệu mẫu đa dạng để đo (evaluation/tao_tai_lieu_mau.py). Pillow còn là
# dependency gián tiếp sẵn có của pdfplumber/python-pptx nên hầu như luôn có sẵn.
reportlab>=4.0
Pillow>=10.0
